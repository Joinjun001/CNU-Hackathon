# 🚀 프로젝트 정의서: Word402 (가제)
> **워드프레스용 x402 프로토콜 기반 AI 에이전트 대상 유료 콘텐츠 공급 플러그인**  
> *A WordPress Plugin for Autonomous Agentic Monetization via x402 & Blockchain Micropayments*

---

## 1. 프로젝트 개요 (Project Overview)

* **프로젝트명 (가제)**: `Word402` (또는 `AgentPress-402`)
* **프로젝트 성격**: 블록체인 캡스톤 프로젝트 (학술 연구 및 실무형 소프트웨어 구현)
* **핵심 슬로건**: *"AI 에이전트를 차단하지 말고, 1원 단위로 판매하라."*
* **한 줄 정의**: 전 세계 웹의 40% 이상을 차지하는 워드프레스(WordPress) 환경에서, 웹사이트 운영자가 별도의 복잡한 결제 계약 없이 설치 한 번으로 **AI 에이전트에게 402 Payment Required를 반환하고 블록체인 마이크로페이먼트로 정산하여 고품질 정제 데이터를 공급하는 플러그인 솔루션**

---

## 2. 문제 정의 및 배경 (Background & Problem)

### 2.1. 웹 콘텐츠 생태계의 패러다임 변화
1. **기존 수익 모델의 붕괴 (Crawl-to-Referral Ratio 급락)**
   - 지난 30년간 웹 미디어와 블로그는 **"검색 노출 → 인간 방문자 유입 → 광고 클릭 / 리퍼럴 수익"** 모델에 의존해 왔습니다.
   - 그러나 LLM 기반 검색(Perplexity, ChatGPT Search, Claude 등)과 자율 AI 에이전트는 사용자를 원문 사이트로 보내지 않고 데이터를 직접 요약 소비합니다.
   - 트래픽과 서버 부하는 늘어나지만, 사이트 운영자의 광고 수익은 증발하는 구조적 위기가 도래했습니다.

2. **기존 대응 방식(차단)의 딜레마**
   - 현재 사이트 운영자들은 `robots.txt` 설정이나 WAF를 통해 AI 봇을 무조건 차단하는 소극적 대응을 취하고 있습니다.
   - 하지만 완전 차단은 AI 생태계에서의 노출 기회 자체를 박탈하므로 장기적인 해결책이 될 수 없습니다.

### 2.2. 해결책: 'AI 에이전트를 위한 무인 계산대'
- AI가 학습하거나 실시간 추론(Inference)에 활용할 수 있도록 데이터를 정제하여 공급하되,
- **에이전트가 접근할 때마다 즉시 대가를 지불하게 만드는 프로토콜 레벨의 유료화 게이트웨이**가 필요합니다.

---

## 3. 핵심 기술 명분 (Why Blockchain & Why x402)

### 3.1. 왜 Web2 결제(신용카드, PG)가 아닌 블록체인인가?
> **"신용카드는 '인간'을 위한 결제망이고, 블록체인은 '머신(AI)'을 위한 유일한 결제망이다."**

| 비교 항목 | 전통 Web2 결제 (신용카드, PG, 간편결제) | 블록체인 결제 레일 (Solana / Base L2) |
| :--- | :--- | :--- |
| **결제 주체** | 법적 자연인 (주민번호, CVC, SMS OTP 필수) | **머신 (Private Key 기반 비대면 자율 전자서명)** |
| **사전 절차** | 사이트 회원가입, 신용카드 등록, 본인인증 강제 | **무허가(Permissionless) 즉시 직거래 (No Account)** |
| **단위 경제학** | 건당 고정 수수료 ($0.30 + a)로 소액 거래 불가 | **건당 수수료 0.01원 미만 (1원~10원 마이크로 결제 성립)** |
| **정산 속도** | D+3 ~ D+7일 정산 주기 | **0.4초 ~ 2초 내 온체인 완결 (Instant Finality)** |
| **국경 장벽** | 국가별 PG 계약 및 복잡한 외환 정산 | **글로벌 단일 통화 (USDC 등 스테이블코인)** |

### 3.2. 왜 x402 프로토콜인가?
- **HTTP 표준의 부활**: 1990년대 초 제정되었으나 결제 인프라의 부재로 잠들어 있던 `HTTP 402 Payment Required` 상태 코드를 부활시킨 최신 오픈 표준입니다.
- **웹 표준 친화적**: 웹 서버와 AI 에이전트 간의 통신에서 복잡한 UI 렌더링 없이, **순수한 HTTP Header/JSON 교환만으로 결제 요구(Challenge)와 영수증(Proof) 제출을 완결**합니다.

---

## 4. 시스템 아키텍처 및 동작 프로세스

### 4.1. 결제 및 데이터 공급 시퀀스 (Flow)

```
[ AI Agent / Crawler ]                 [ WordPress + Word402 Plugin ]         [ Blockchain / Facilitator ]
        │                                           │                                     │
        │─── 1. GET /wp-json/wp/v2/posts/{id} ─────>│                                     │
        │    (Accept: application/json or text/md)  │                                     │
        │                                           │                                     │
        │<── 2. HTTP 402 Payment Required ──────────│                                     │
        │    Headers:                               │                                     │
        │      x402-recipient: 0x... (또는 Solana)    │                                     │
        │      x402-price: 0.005 USDC               │                                     │
        │      x402-network: base-sepolia (or solana)│                                    │
        │      x402-challenge: <nonce/invoice_id>   │                                     │
        │                                           │                                     │
        │─── 3. 자율 결제 트랜잭션 서명 및 송금 ───────────────────────────────────────────>│
        │                                           │                                     │
        │─── 4. 재요청 (결제 증명 첨부) ────────────>│                                     │
        │    Header: X-PAYMENT: {tx_sig, nonce}     │                                     │
        │                                           │─── 5. 온체인 영수증 검증 (/verify) ─>│
        │                                           │<── 6. 수취 확인 및 Replay 방지 ─────│
        │                                           │                                     │
        │<── 7. HTTP 200 OK ────────────────────────│                                     │
        │    (HTML 노이즈 없는 클린 Markdown 본문)    │                                     │
```

---

## 5. 시스템 주요 기능 (Key Features)

### 5.1. WordPress 플러그인 (`Word402`)
1. **에이전트 접근 가로채기 (Request Interceptor)**:
   - WP Hook(`template_redirect`, `rest_pre_dispatch`)을 사용하여 일반 유저의 웹 브라우저 접근은 정상 허용하고, AI 에이전트 접근 시 `HTTP 402` 반환.
2. **관리자 설정 페이지 (WP Admin Dashboard)**:
   - 정산받을 지갑 주소(EVM / Solana) 등록.
   - 글/카테고리별 단가 설정 (예: 일반 포스트 0.001 USDC, 전문 분석 리포트 0.05 USDC).
   - 에이전트 결제 통계 (누적 호출 수, 누적 수익액, 실시간 정산 내역).
3. **온체인 영수증 검증 및 재사용(Replay Attack) 방지**:
   - 에이전트가 보낸 트랜잭션 서명(Signature/Tx Hash)을 RPC 노드 또는 Facilitator를 통해 실시간 조회하여 금액 및 수취인 일치 여부 확인.
   - 사용된 영수증은 DB/캐시에 기록하여 단일 결제 건으로 무한 크롤링하는 행위 방지.
4. **AI 최적화 데이터 서빙 (Clean Markdown Engine)**:
   - 광고, 사이드바, 네비게이션 등 불필요한 HTML을 파싱 제거하고, LLM이 이해하기 가장 효율적인 **정제된 Markdown 형식**으로 본문 서빙.

### 5.2. AI 에이전트 클라이언트 (시연 및 검증용)
- Python / TypeScript 기반의 자율형 리서치 에이전트 CLI.
- 워드프레스 엔드포인트 호출 → 402 에러 수신 → 자체 지갑 잔액으로 소액 서명 및 전송 → 재요청하여 Markdown 데이터 취득 후 최종 리포트 출력 데모.

### 5.3. 결제 게이트웨이 연계 (기존 인프라 활용)
- 보유 중인 블록체인 결제 시스템(GeyserPayment / gsspay)의 지갑 풀(Wallet Pool), 트랜잭션 감지 및 컨펌 상태 관리 엔진을 x402 Facilitator 백엔드로 연동/확장 가능.

---

## 6. 기대 효과 및 캡스톤 차별성

1. **실현 가능성 및 데모 완성도**:
   - 워드프레스 생태계와 표준 HTTP 코드를 활용하므로 심사위원 및 청중이 직관적으로 이해할 수 있는 실물 E2E 데모 시연 가능.
2. **블록체인 당위성 충족**:
   - "왜 굳이 블록체인을 썼는가?"라는 질문에 대해 **머신 간 무인증 결제(Autonomous M2M), 1원 단위 마이크로페이먼트, 즉각적 글로벌 정산**이라는 완벽한 기술적 해답을 제시.
3. **시장 파급력**:
   - Cloudflare와 같은 거대 CDN 인프라 기업의 독점 솔루션에 의존하지 않고, **오픈소스 워드프레스 플러그인만으로 누구나 셀프 호스팅형 AI 유료 데이터 게이트웨이를 구축**할 수 있는 웹 민주화 기여.
